{
  "_schema": {
    "name":             "short id used in results table",
    "urls":             "map of transport → URL; keys: raw-quic, h3-wt",
    "drafts":           "list of MoQT draft numbers (14, 16)",
    "pub_mode":         "publisher messages: publish | publish-ns | publish-both",
    "insecure":         "bool; if true, pass --tls-disable-verify / -k (default false)",
    "disabled":         "bool; if true, skip this relay entirely (default false)",
    "disabled_suites":  "list of interop suite names to skip for this relay",
    "notes":            "freeform (optional)"
  },
  "relays": [
    {
      "name": "moqx-main",
      "urls": {
        "raw-quic": "moqt://moqx-main.ci.openmoq.org:4433",
        "h3-wt":    "https://moqx-main.ci.openmoq.org:4433/moq-relay"
      },
      "drafts": [14, 16],
      "pub_mode": "publish",
      "disabled_suites": []
    },
    {
      "name": "cloudflare-d14",
      "urls": {
        "raw-quic": "moqt://draft-14.cloudflare.mediaoverquic.com:443"
      },
      "drafts": [14],
      "pub_mode": "publish-both",
      "disabled_suites": ["relay-fetch"],
      "notes": "CF d14 moq-rs: publisher must send PUB_NS + PUBLISH. relay-fetch disabled (#24): this d14 build does not answer standalone FETCH (TimeoutError — unsupported, will never pass); join works."
    },
    {
      "name": "moxygen-fb",
      "urls": {
        "raw-quic": "moqt://fb.mvfst.net:9448",
        "h3-wt":    "https://fb.mvfst.net:9448/moq-relay"
      },
      "drafts": [14, 16],
      "pub_mode": "publish",
      "disabled_suites": [],
      "notes": "Upstream moxygen reference relay"
    },
    {
      "name": "moq-dev-anon",
      "urls": {
        "h3-wt": "https://cdn.moq.dev:443/anon"
      },
      "drafts": [14, 16],
      "pub_mode": "publish",
      "disabled": true,
      "disabled_suites": ["relay-join", "relay-fetch"],
      "notes": "Subscriber-only anon endpoint: publish is rejected at QUIC (code 91141958510816), SUBSCRIBE returns non-spec code=404 Broadcast-not-found. Re-enable if a write-capable path becomes available."
    },
    {
      "name": "quichemoq",
      "urls": {
        "h3-wt": "https://quichemoq.dev:443"
      },
      "drafts": [14, 16],
      "pub_mode": "publish",
      "disabled": true,
      "disabled_suites": ["relay-join", "relay-fetch"],
      "notes": "Every connect drops with bare ConnectionError (no QUIC code, no reason). Possibly not MoQT-speaking on this port. Re-probe before enabling."
    },
    {
      "name": "quicr-west",
      "urls": {
        "raw-quic": "moqt://us-west-2.relay.quicr.org:33437",
        "h3-wt":    "https://us-west-2.relay.quicr.org:33437/relay"
      },
      "drafts": [16],
      "pub_mode": "publish",
      "insecure": true,
      "compat": ["libquicr"],
      "disabled_suites": [],
      "notes": "Cisco libquicr (server cert unacceptable → insecure). d14 dropped (raw + WT now unresponsive — relay appears d16-only). compat=libquicr: relay returns SUBSCRIBE_OK for a nonexistent track (deferred-delivery policy), tolerated + annotated."
    },
    {
      "name": "moqtail",
      "urls": {
        "h3-wt": "https://relay.moqtail.dev"
      },
      "drafts": [14],
      "pub_mode": "publish",
      "disabled": true,
      "disabled_suites": ["relay-join", "relay-fetch"],
      "notes": "OzU moqtail (Rust), WT d14 only. Disabled: WT CONNECT refused (code=0) at the catalog URL (no :path). Re-enable once the correct WebTransport path is known / relay confirmed up."
    },
    {
      "name": "imquic",
      "urls": {
        "raw-quic": "moqt://lminiero.it:9000",
        "h3-wt":    "https://lminiero.it:9000"
      },
      "drafts": [16],
      "pub_mode": "publish",
      "disabled_suites": [],
      "notes": "Meetecho imquic (C). d14 probes rejected (WT: \"No WebTransport protocol offered\"; QUIC close 376); only d16 tested here."
    },
    {
      "name": "cf-d16-interop",
      "urls": {
        "raw-quic": "moqt://draft-16-manish.cloudflare.mediaoverquic.com:443"
      },
      "drafts": [16],
      "pub_mode": "publish",
      "compat": ["lenient-extensions"],
      "disabled_suites": ["relay-join", "relay-fetch"],
      "notes": "Cloudflare moq-rs draft-16 interop branch. compat=lenient-extensions: this fork emits a truncated trailing-extensions KVP block on SubscribeOk (kvp_start=6 tell=7 exts_end=7) which our strict parser rejects as non-compliant; tolerated + annotated, recovering ctrl-msg to 6/6. relay-join + relay-fetch disabled (#24): the relay never answers JOIN/FETCH (TimeoutError — unsupported, will never pass). pub-sub[publish] kept as a genuine fail: subscribers get code=4 'Track not found' (publisher's track not visible — a moq-rs publish-routing difference; the same multi_sub_bench flow passes vs moxygen/imquic). Upstream also advertises an H3/WT endpoint at https://.../moq but our client hits a SETUP parse error (\"Read out of bounds\") — QUIC only here until that's resolved."
    }
  ]
}
